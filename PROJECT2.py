#it is a general extension of the message sender.
#it can be used in any project.
#as after sending the message it must shutdown the system automatically.

import os 

os.system("shutdown /s  /t 15") #it will shutdown the system  automatically after 15sec